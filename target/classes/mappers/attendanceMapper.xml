<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.attmng.atms.mappers.attendance">

	<select id="getAttendanceInfo" parameterType="map" resultType="AttendanceDTO">
		 SELECT * FROM t_attendance
		 WHERE id = #{id}
		 AND key_year_month = #{month}
		 AND del_cd = #{flag}
	</select>
	
	<update id="attendanceUpdate" parameterType="map">
		UPDATE t_attendance 
		SET s_time= # {upd_date}, upd_id = #{upd_id}
		WHERE id= #{id} 
		AND key_year_month= #{month} 
		AND key_day= #{day}
		AND del_cd = #{flag}
	</update>
	<!-- 
	<select id="getExcelInfo" parameterType="map" resultType="AttendanceDTO">
		
		
	</select>
	
	<insert id="insertAttendanceInfo">
		
	</insert>
	 -->
	
</mapper>